#compdef _robo robo

_robo () {
    _alternative 'command:command:_robo_get_command_list'
}
	
_robo_get_command_list () {
    if type "robo" > /dev/null; then
        robo \
        	| sed "1,/Available commands/d" \
    	    | awk '/^ +[a-z:]+/ { print $1 }'
    fi
}

